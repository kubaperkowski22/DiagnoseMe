## Configuration

The application requires the following environment variables:

- `OPENAI_API_KEY` — API key used for LLM requests.
- `DIAGNOSEME_CONNECTION_STRING` — SQL Server connection string.

No credentials are included in the repository. The original cloud resources
used by the academic prototype have been decommissioned.

The application requires separately configured services to run. It is not
currently maintained as a live medical service.