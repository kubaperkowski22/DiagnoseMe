<UserControl x:Class="DiagnoseMe.Views.LogInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiagnoseMe.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="90"/>
            <RowDefinition Height="90"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Zaloguj się"
                   Grid.Row="0"
                   Style="{StaticResource HeaderTextStyle}"
                   HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center">
            <TextBlock Text="Email"
                       Style="{StaticResource PrimaryTextStyle}"/>
            <TextBox Width="400"/>

        </StackPanel>

        <StackPanel Grid.Row="2"
                    HorizontalAlignment="Center">
            <TextBlock Text="Password"
               Style="{StaticResource PrimaryTextStyle}"/>
            <PasswordBox Width="400"
                         PasswordChar="⚫"/>

        </StackPanel>

        <WrapPanel Grid.Row="3"
                   HorizontalAlignment="Center">
            <Button Style="{StaticResource RegularButton}"
                    Width="130"
                    Height="30"
                    Margin="15"
                    Content="Zaloguj"
                    Click="LogIn_Button_Click"/>
            <Button Style="{StaticResource RegularButton}"
                    Width="130"
                    Height="30"
                    Content="Stwórz konto"
                    Click="Register_Button_Click"/>
            <Button Style="{StaticResource RegularButton}"
                    Width="140"
                    Height="30"
                    Margin="15"
                    Content="Zapomniałem/am hasła"
                    Click="ForgotPassword_Button_Click">
                
            </Button>
        </WrapPanel>

    </Grid>
</UserControl>
